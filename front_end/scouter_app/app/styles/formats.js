import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    nextButton: {
        backgroundColor: 'red',
        width: '100%',
        height: 50,
        alignItems: 'center'
    },
    nextButtonText: {
        fontSize: 20,
        fontWeight: "bold"
    }
})
